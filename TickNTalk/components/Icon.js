import React from "react";
import { Icon } from "@ui-kitten/components";

export const Back = (props) => (
    <Icon name="arrow-back" pack="ionicon" {...props} />
);

export const Add = (props) => (
    <Icon name="add-circle-outline" pack="ionicon" {...props}/>
);