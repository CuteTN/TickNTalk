import React from "react";
import { Icon } from "@ui-kitten/components";

export const Back = (props) => (
    <Icon name="arrow-back" pack="ionicon" {...props} />
);

export const AddCircle = (props) => (
    <Icon name="add-circle-outline" pack="ionicon" {...props}/>
);

export const Send = (props) => (
    <Icon name="send" pack="ionicon" {...props}/>
);

export const Add = (props) => (
    <Icon name="add" pack="ionicon" {...props}/>
);

export const Camera = (props) => (
    <Icon name="camera" pack="ionicon" {...props}/>
);

export const Image = (props) => (
    <Icon name="image" pack="ionicon" {...props}/>
);

export const Call = (props) => (
    <Icon name="call" pack="ionicon" {...props}/>
);

export const VideoCam = (props) => (
    <Icon name="videocam" pack="ionicon" {...props}/>
);
export const Voice = (props) => (
    <Icon name="mic" pack="ionicon" {...props}/>
);
export const StopVoice = (props) => (
    <Icon name="stop-circle-outline" pack="ionicon" {...props}/>
);
export const Play = (props) => (
    <Icon name="play" pack="ionicon" {...props}/>
);
export const Stop = (props) => (
    <Icon name="md-stop-circle-outline" pack="ionicon" {...props}/>
);